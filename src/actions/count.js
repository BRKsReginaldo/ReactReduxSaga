import * as types from '../actionTypes/count'

export const increment = () => ({type: types.increment})
export const incrementAsync = () => ({type: types.incrementAsync})
export const decrement = () => ({type: types.decrement})
